import { ThreePlaneGeometryDirective } from './three-plane-geometry.directive';

describe('ThreePlaneGeometryDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreePlaneGeometryDirective();
    expect(directive).toBeTruthy();
  });
});
