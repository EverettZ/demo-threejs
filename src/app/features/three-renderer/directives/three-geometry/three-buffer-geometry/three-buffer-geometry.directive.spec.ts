import { ThreeBufferGeometryDirective } from './three-buffer-geometry.directive';

describe('ThreeBufferGeometryDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeBufferGeometryDirective();
    expect(directive).toBeTruthy();
  });
});
