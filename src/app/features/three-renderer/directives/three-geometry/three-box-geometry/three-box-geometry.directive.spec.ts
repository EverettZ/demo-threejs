import { BoxGeometryDirective } from './three-box-geometry.directive';

describe('BoxGeometryDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxGeometryDirective();
    expect(directive).toBeTruthy();
  });
});
