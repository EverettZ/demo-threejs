import { ThreeLambertMaterialDirective } from './three-lambert-material.directive';

describe('ThreeLambertMaterialDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeLambertMaterialDirective();
    expect(directive).toBeTruthy();
  });
});
