import { StandardMaterialDirective } from './standard-material.directive';

describe('StandardMaterialDirective', () => {
  it('should create an instance', () => {
    const directive = new StandardMaterialDirective();
    expect(directive).toBeTruthy();
  });
});
