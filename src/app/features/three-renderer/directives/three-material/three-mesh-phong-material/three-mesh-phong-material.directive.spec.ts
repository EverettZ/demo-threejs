import { ThreeMeshPhongMaterialDirective } from './three-mesh-phong-material.directive';

describe('ThreeMeshPhongMaterialDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeMeshPhongMaterialDirective();
    expect(directive).toBeTruthy();
  });
});
