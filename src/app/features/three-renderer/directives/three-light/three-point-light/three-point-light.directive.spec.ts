import { ThreePointLightDirective } from './three-point-light.directive';

describe('ThreePointLightDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreePointLightDirective();
    expect(directive).toBeTruthy();
  });
});
