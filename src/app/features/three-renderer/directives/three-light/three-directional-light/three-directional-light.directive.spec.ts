import { ThreeDirectionalLightDirective } from './three-directional-light.directive';

describe('ThreeDirectionalLightDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeDirectionalLightDirective();
    expect(directive).toBeTruthy();
  });
});
