import { ThreeAmbientLightDirective } from './three-ambient-light.directive';

describe('ThreeAmbientLightDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeAmbientLightDirective();
    expect(directive).toBeTruthy();
  });
});
