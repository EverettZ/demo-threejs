import { ThreePointsDirective } from './three-points.directive';

describe('ThreePointsDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreePointsDirective();
    expect(directive).toBeTruthy();
  });
});
