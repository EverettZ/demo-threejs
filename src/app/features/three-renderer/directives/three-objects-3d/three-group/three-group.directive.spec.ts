import { ThreeGroupDirective } from './three-group.directive';

describe('ThreeGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeGroupDirective();
    expect(directive).toBeTruthy();
  });
});
