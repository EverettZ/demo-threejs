import { ThreeOrbitControlsDirective } from './three-orbit-controls.directive';

describe('ThreeOrbitControlsDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeOrbitControlsDirective();
    expect(directive).toBeTruthy();
  });
});
